<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/myAccountLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".MyAccount">

    <!-- Toolbar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar3"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="#000000"
        android:theme="?attr/actionBarTheme"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Título Principal -->
    <TextView
        android:id="@+id/textView49"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="MINHA CONTA"
        android:textColor="#03A9F4"
        android:textSize="20sp"
        app:layout_constraintBottom_toBottomOf="@id/toolbar3"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.498"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/toolbar3" />

    <!-- Título "Bem-vindo" -->
    <TextView
        android:id="@+id/textView50"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="28dp"
        android:text="Bem-vindo!"
        android:textColor="#34C9FF"
        android:textSize="30sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.498"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/toolbar3" />

    <!-- Centralizado: Dados, Pedidos e Endereço -->
    <LinearLayout
        android:layout_width="251dp"
        android:layout_height="250dp"
        android:layout_marginTop="120dp"
        android:gravity="center"
        android:orientation="vertical"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.496"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/textView50">

        <!-- Meus Dados com Ícone -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <!-- Ícone -->
            <ImageView
                android:id="@+id/myDataIcon"
                android:layout_width="44dp"
                android:layout_height="33dp"
                android:contentDescription="Ícone de dados"
                android:src="@drawable/ic_baseline_person_24"
                app:tint="#34C9FF" />

            <!-- Espaço entre a imagem e o texto -->
            <Space
                android:layout_width="8dp"
                android:layout_height="wrap_content" />

            <!-- Texto -->
            <TextView
                android:id="@+id/myDocuments"
                android:layout_width="125dp"
                android:layout_height="wrap_content"
                android:text="Meus dados"
                android:textColor="#000000"
                android:textSize="20sp"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- Linha separadora -->

        <!-- Meus Pedidos com Ícone -->
        <View
            android:layout_width="match_parent"
            android:layout_height="3dp"
            android:layout_marginVertical="12dp"
            android:background="#DCDCDE" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <!-- Ícone -->
            <ImageView
                android:id="@+id/myOrdersIcon"
                android:layout_width="41dp"
                android:layout_height="33dp"
                android:contentDescription="Ícone de pedidos"
                android:src="@drawable/box_unpacked"
                app:tint="#34C9FF" />

            <!-- Espaço entre a imagem e o texto -->
            <Space
                android:layout_width="8dp"
                android:layout_height="wrap_content" />

            <!-- Texto -->
            <TextView
                android:id="@+id/myOrdersTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Meus pedidos"
                android:textColor="#000000"
                android:textSize="20sp"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- Linha separadora -->
        <View
            android:layout_width="match_parent"
            android:layout_height="3dp"
            android:layout_marginVertical="12dp"
            android:background="#DCDCDE" />

        <!-- Meu Endereço com Ícone -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <!-- Ícone -->
            <ImageView
                android:id="@+id/imageView11"
                android:layout_width="42dp"
                android:layout_height="33dp"
                android:contentDescription="Ícone referente ao endereço"
                android:src="@drawable/location_icon"
                app:tint="#34C9FF" />

            <!-- Espaço entre a imagem e o texto -->
            <Space
                android:layout_width="8dp"
                android:layout_height="wrap_content" />

            <!-- Texto -->
            <TextView
                android:id="@+id/myAddress"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Meu endereço"
                android:textColor="#000000"
                android:textSize="20sp"
                android:textStyle="bold" />
        </LinearLayout>

    </LinearLayout>

    <!-- Bottom Navigation View -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigationView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@android:color/black"
        app:itemIconTint="@android:color/white"
        app:itemTextColor="@android:color/white"
        app:menu="@menu/bottom_nav_menu"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
